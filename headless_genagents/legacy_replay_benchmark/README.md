# Legacy Replay Benchmark

These scripts implement the earlier trace-guided replay benchmark path. They are
kept for reference, but the current benchmark flow uses `run_grouped_trace_llm.py`
with `runs/<sim>/trace.jsonl`, `position_index.jsonl`, and `group_index.jsonl`.

